<div class="trickle {{_id}} {{_trickle._button._component}}{{#if _isButtonLocking}} is-locked{{/if}}{{#if _trickle._button._isFullWidth}} is-full-width{{/if}}{{#if _trickle._button._className}} {{_trickle._button._className}}{{/if}}">

  {{{a11y_aria_label _globals._extensions._trickle.incompleteContent}}}

  <div class="trickle__inner js-trickle-btn-container {{_trickle._button._component}}__inner {{#if _isButtonLocking}} is-locked{{/if}}{{#unless _isButtonVisible}} u-display-none{{/unless}}">

    <button class="trickle__btn js-trickle-btn {{#if _isButtonDisabled}} is-disabled{{/if}}{{#if _trickle._button._hasIcon}} btn-icon{{/if}}{{#any _trickle._button.finalText _trickle._button.text _trickle._button.startText}} btn-text{{/any}}"{{#if _isButtonDisabled}} disabled="disabled"{{/if}}>

      {{#if _trickle._button._hasIcon}}
      <div class="trickle__btn-icon">
        <div class="icon"></div>
      </div>
      {{/if}}

      {{#any _trickle._button.finalText _trickle._button.text _trickle._button.startText}}
      <div class="trickle__btn-text">
        <div class="trickle__btn-text-inner">
          {{#if _isButtonStart}}
            {{#if _trickle._button.startText}}
              {{{_trickle._button.startText}}}
            {{else}}
              {{{_trickle._button.text}}}
            {{/if}}
          {{else}}
            {{#if _isButtonFinal}}
              {{#if _trickle._button.finalText}}
                {{{_trickle._button.finalText}}}
              {{/if}}
            {{else}}
              {{{_trickle._button.text}}}
            {{/if}}
          {{/if}}
        </div>
      </div>
      {{/any}}

    </button>

  </div>

</div>
